| ID | Severity | Status | File:line | Impact | Operational failure scenario | Remediation |
|---|---|---|---|---|---|---|
| P0-01 | critical | confirmed | `Sources/TimedKit/Core/Clients/SupabaseEndpoints.swift:18`<br>`Sources/TimedKit/Core/Clients/SupabaseEndpoints.swift:22`<br>`Sources/TimedKit/Core/Clients/SupabaseEndpoints.swift:31`<br>`supabase/functions/anthropic-proxy/index.ts:7`<br>`supabase/functions/anthropic-proxy/index.ts:23`<br>`supabase/functions/anthropic-proxy/index.ts:31` | The public anon key embedded in the app can call a generic Anthropic proxy that forwards arbitrary request bodies while injecting the server Anthropic key. | Anyone extracting the anon key from the binary can send arbitrary `/v1/messages` payloads, choose model, token count, tool use, and streaming behavior, then burn production Anthropic spend outside the app. | Require a user session JWT with `verifyAuth`; reject anon-only calls; enforce server-side model, max token, tool, and request shape allowlists; add per-user quota/rate limits; consider replacing the generic proxy with narrow feature-specific functions. |
| P0-02 | critical | confirmed | `scripts/ralph-telegram.ts:23` | A live Telegram bot token is committed in a tracked repo file. | Any repo reader or leaked clone can control the bot and issue operational commands such as skip, stop, resume, or add through the bot API. | Revoke the token; move the replacement token to an environment secret; fail startup when the token is absent; make gitleaks blocking before merge. |
| P0-03 | critical | confirmed | `docker-compose.local.yml:12`<br>`docker-compose.local.yml:18`<br>`docker-compose.local.yml:31`<br>`docker-compose.local.yml:33`<br>`docker-compose.local.yml:34`<br>`docker-compose.local.yml:54` | The ignored local Docker compose file contains literal MCP tokens, Supabase service-role keys, Voyage key, and Anthropic key values. | If this workspace, backup, screen share, crash report, or local history leaves the machine, an attacker can bypass RLS with service-role access and run provider billing through production keys. | Rotate every literal key if the file ever left the machine; replace values with `${ENV_VAR}` references; keep only a `.example` file in repo; store real values in a secret manager or platform secrets. |
| P1-01 | high | confirmed | `Sources/TimedKit/Core/Clients/SupabaseEndpoints.swift:31`<br>`supabase/functions/elevenlabs-tts-proxy/index.ts:7`<br>`supabase/functions/elevenlabs-tts-proxy/index.ts:24`<br>`supabase/functions/elevenlabs-tts-proxy/index.ts:37`<br>`supabase/functions/elevenlabs-tts-proxy/index.ts:40` | The ElevenLabs proxy is anon-callable and lets callers choose text, voice, and model while the server injects the paid API key. | Extracted anon key traffic can generate arbitrary TTS audio, exhaust ElevenLabs quota, and make real users lose voice features. | Require user JWT auth; cap text length; whitelist voice IDs and models; add per-user quotas and rate limits; return sanitized errors. |
| P1-02 | high | confirmed | `Sources/TimedKit/Features/Onboarding/VoiceOnboardingView.swift:223`<br>`Sources/TimedKit/Features/Onboarding/VoiceOnboardingView.swift:237`<br>`Sources/TimedKit/Features/Onboarding/VoiceOnboardingView.swift:242`<br>`Sources/TimedKit/Features/Onboarding/VoiceOnboardingView.swift:244`<br>`supabase/functions/extract-onboarding-profile/index.ts:61`<br>`Sources/TimedKit/Features/MorningCheckIn/MorningCheckInManager.swift:153`<br>`Sources/TimedKit/Features/MorningCheckIn/MorningCheckInManager.swift:158`<br>`Sources/TimedKit/Features/MorningCheckIn/MorningCheckInManager.swift:164`<br>`supabase/functions/extract-voice-learnings/index.ts:62` | Onboarding and voice-learning calls use the anon header and swallow network or HTTP failures, while both Edge Functions require `verifyAuth`. | The UI marks onboarding complete locally, but the backend profile, `onboarded_at`, and learning extraction never persist; the product then behaves as if the user gave no context. | Route calls through `EdgeFunctions.makeRequest` or another session-JWT path; check non-2xx responses; show a recoverable degraded state; only mark onboarding complete after persistence succeeds or is durably queued. |
| P1-03 | high | confirmed | `Sources/TimedKit/Core/Services/OfflineSyncQueue.swift:79`<br>`Sources/TimedKit/Core/Services/OfflineSyncQueue.swift:86`<br>`Sources/TimedKit/Core/Services/GracefulDegradation.swift:116`<br>`Sources/TimedKit/Core/Services/GracefulDegradation.swift:117` | The offline queue has a monitor/flush API, but no app startup path registers the execution handler; the recovery hook only logs. | Email, calendar, and Tier 0 observations queued while offline remain stranded in SQLite after network recovery, so Supabase and downstream intelligence never see them. | Register a single operation dispatcher at app launch; flush on launch and network restoration; add operation handlers for every queued operation type; expose pending and permanently failed counts in diagnostics. |
| P1-04 | high | confirmed | `Sources/TimedKit/Core/Services/DataBridge.swift:24`<br>`Sources/TimedKit/Core/Services/DataBridge.swift:27`<br>`Sources/TimedKit/Core/Services/DataBridge.swift:29`<br>`Sources/TimedKit/Core/Services/DataBridge.swift:58`<br>`Sources/TimedKit/Core/Services/DataBridge.swift:61` | Task writes fall back to local JSON when offline and fire-and-forget Supabase upserts when online, but failed remote writes are only logged and never queued. | A user edits tasks during a transient Supabase outage; local state looks correct, but cross-device data and backend planning state permanently diverges. | Add durable task mutation records with idempotency keys; queue remote write failures; replay on recovery; surface pending sync status to the user. |
| P1-05 | high | confirmed | `Sources/TimedKit/Core/Services/EmailSyncService.swift:407`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:430`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:447`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:454`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:459` | Microsoft Graph delta processing catches per-message failures but still advances and persists the delta link. | One message fails during upsert, Tier 0 observation, folder move detection, or classification trigger; Graph will not replay it after the delta cursor advances, causing silent inbox signal loss. | Track failed message IDs; do not advance the cursor until failed rows are durably stored or queued; add a retry/dead-letter table and tests for a single failed message in a successful page. |
| P1-06 | high | confirmed | `Sources/TimedKit/Core/Services/GmailSyncService.swift:202`<br>`Sources/TimedKit/Core/Services/GmailSyncService.swift:241`<br>`Sources/TimedKit/Core/Services/GmailSyncService.swift:248`<br>`Sources/TimedKit/Core/Services/GmailSyncService.swift:326`<br>`Sources/TimedKit/Core/Services/GmailSyncService.swift:329`<br>`Sources/TimedKit/Core/Clients/GmailClient.swift:297`<br>`Sources/TimedKit/Core/Clients/GmailClient.swift:302`<br>`Sources/TimedKit/Core/Clients/GmailClient.swift:305` | Gmail delta and backfill paths swallow hydration/upsert failures and still seed or advance `historyId`. | Gmail rate limits, malformed messages, or Supabase errors during sync cause messages to be skipped forever while the cursor moves past them. | Make persistence failures throw; only advance `historyId` after all messages are stored or queued; retry 429/5xx with backoff; persist failed Gmail IDs for replay. |
| P1-07 | high | confirmed | `Sources/TimedKit/Core/Services/EmailSyncService.swift:293`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:317`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:318`<br>`Sources/TimedKit/Core/Services/EmailSyncService.swift:320`<br>`supabase/migrations/20260411000000_executives_table.sql:16`<br>`supabase/migrations/20260411000000_executives_table.sql:19`<br>`supabase/migrations/20260427000000_email_sync_driver.sql:10` | The client-side/server-side sync gate reads the RLS-protected `executives` row with the anon key and fails open to client polling. | After an executive is flipped to `email_sync_driver = 'server'`, Swift may fail to read the flag and continue Graph polling, racing Trigger.dev server-side ingestion. | Read the flag with the current user JWT or a narrow authenticated function; fail visibly instead of silently defaulting to client; add a regression test for the `server` driver path. |
| P1-08 | high | confirmed | `scripts/package_app.sh:5`<br>`scripts/package_app.sh:6`<br>`scripts/package_app.sh:150`<br>`scripts/package_app.sh:152`<br>`scripts/notarize_app.sh:5`<br>`scripts/notarize_app.sh:6`<br>`scripts/notarize_app.sh:9`<br>`scripts/notarize_app.sh:30`<br>`scripts/notarize_app.sh:36` | Packaging emits `dist.noindex/Timed.app` with ad-hoc signing, while notarization expects `dist/timed.app`. | A release candidate built by the packaging script cannot be notarized by the notarization script; users receive an unsigned or unnotarized app that fails Gatekeeper expectations. | Use one shared artifact path and app name; sign with Developer ID and hardened runtime; staple the app or DMG; gate release on `codesign`, `spctl`, and `notarytool` success. |
| P2-01 | medium | confirmed | `trigger/src/tasks/ingestion-health-watchdog.ts:30`<br>`trigger/src/tasks/ingestion-health-watchdog.ts:33`<br>`trigger/src/tasks/ingestion-health-watchdog.ts:169`<br>`trigger/src/tasks/ingestion-health-watchdog.ts:243` | The ingestion watchdog is callable on demand, not scheduled, even though it is the task that emits alert-bearing records for stale ingestion and failed agent sessions. | Email/calendar ingestion can stop for hours and no production alert fires unless a human or external job manually invokes the task. | Restore a schedule when the Trigger plan supports it, or invoke it from an external monitor; add an alert rule that pages on returned or logged `alert: true`. |
| P2-02 | medium | confirmed | `supabase/functions/pipeline-health-check/index.ts:330`<br>`supabase/functions/pipeline-health-check/index.ts:337`<br>`supabase/functions/pipeline-health-check/index.ts:348`<br>`supabase/functions/pipeline-health-check/index.ts:379`<br>`supabase/functions/pipeline-health-check/index.ts:396` | Pipeline health returns `ok` placeholders for backfill and nightly pipeline checks, and webhook alerting is optional when the webhook env var is absent. | Operators see a green health response while core scheduled intelligence is not actually checked, and warning rows may never leave Supabase logs. | Replace placeholders with real checks; require or explicitly document alert-webhook configuration; fail health checks when critical probes are unimplemented. |
| P2-03 | medium | confirmed | `.github/workflows/macos-ci.yml:35`<br>`.github/workflows/macos-ci.yml:42`<br>`scripts/package_app.sh:5`<br>`scripts/package_app.sh:6` | CI uploads `dist/timed.app`, but the packaging script creates `dist.noindex/Timed.app`. | CI can complete the package step and then fail artifact upload or publish no usable app bundle for review. | Align CI artifact path with the package script; upload the same artifact used for signing/notarization; add artifact existence checks before upload. |
| P2-04 | medium | confirmed | `.github/workflows/security.yml:18`<br>`.github/workflows/security.yml:21`<br>`.github/workflows/security.yml:31`<br>`.github/workflows/security.yml:33`<br>`.github/workflows/security.yml:45`<br>`.github/workflows/security.yml:52`<br>`.github/workflows/security.yml:78`<br>`.github/workflows/security.yml:82` | Security scanning is explicitly advisory/report-only, including gitleaks and semgrep. | The tracked Telegram token would not block merge or release; future secrets and high-confidence security findings can continue into protected branches. | Make gitleaks blocking immediately on `unified` and `main`; remove `|| true` from semgrep after baseline; keep only noisy tools in advisory mode. |
| P2-05 | medium | confirmed | `.github/workflows/macos-ci.yml:19`<br>`.github/workflows/macos-ci.yml:23`<br>`project.yml:55`<br>`project.yml:56`<br>`project.yml:59` | CI validates SwiftPM build/test and Deno checks, but it does not generate or build the Xcode project that owns app Info.plist, entitlements, hardened runtime, signing, and platform targets. | `swift build` passes while the actual macOS app target, generated project, iOS target, entitlements, or framework embedding is broken. | Add CI jobs for `xcodegen generate`, macOS app `xcodebuild`, iOS simulator build if supported, and package/notarization smoke checks. |
| P2-06 | medium | needs verification | `supabase/migrations/20260413000000_service_role_rls_bypass.sql:98`<br>`supabase/migrations/20260413000000_service_role_rls_bypass.sql:100`<br>`supabase/migrations/20260414000004_acb_refresh_cron.sql:9`<br>`supabase/migrations/20260414000004_acb_refresh_cron.sql:11`<br>`supabase/migrations/20260414000005_weekly_avoidance_cron.sql:9`<br>`supabase/migrations/20260414000005_weekly_avoidance_cron.sql:11` | pg_cron migrations use two different GUC namespaces: `app.supabase_url`/`app.service_role_key` and `app.settings.supabase_url`/`app.settings.service_role_key`. | If only one namespace is configured in the remote database, some cron jobs fail at runtime with missing `current_setting` values and silently stop invoking Edge Functions. | Verify remote GUCs and cron run history; standardize on one namespace; add `current_setting(..., true)` guardrails and an operational check for last successful cron call. |
| P2-07 | medium | confirmed | `scripts/package_app.sh:78`<br>`scripts/package_app.sh:99`<br>`scripts/package_app.sh:136`<br>`project.yml:55`<br>`Platforms/Mac/Info.plist:23`<br>`Platforms/Mac/Info.plist:26` | The packaging script writes a duplicate hardcoded Info.plist instead of using the canonical Xcode plist. | Release bundles can drift from the app target in version, URL schemes, usage strings, app icon metadata, or future entitlements-sensitive metadata. | Package from the Xcode-built app or copy/process `Platforms/Mac/Info.plist`; keep version and bundle metadata in one source of truth. |
| P2-08 | medium | confirmed | `supabase/functions/generate-embedding/index.ts:1`<br>`supabase/functions/generate-embedding/index.ts:24`<br>`supabase/functions/generate-embedding/index.ts:28`<br>`BUILD_STATE.md:127`<br>`BUILD_STATE.md:128` | Operational docs say dual-provider embeddings are active, but the local `generate-embedding` function is disabled and returns empty embeddings. | Operators debugging retrieval quality, Graphiti backfill, or memory ranking may assume embeddings are live when this function returns dimension `0`. | Reconcile docs with deployed behavior; either remove the stale active-embedding claim or re-enable the function; add a health probe that asserts expected embedding dimension. |
| P2-09 | medium | confirmed | `BUILD_STATE.md:116`<br>`BUILD_STATE.md:117`<br>`BUILD_STATE.md:118`<br>`BUILD_STATE.md:119`<br>`BUILD_STATE.md:120` | Production-critical sync and memory paths are explicitly not covered by tests. | Cursor advancement, offline replay, voice extraction, and memory persistence regressions can ship without CI catching them. | Add focused tests for EmailSyncService cursor behavior, Gmail history failures, OfflineSyncQueue replay, voice extraction auth failure, memory store persistence, and reflection engine outputs. |
| P2-10 | medium | confirmed | `services/graphiti/app/main.py:118`<br>`services/graphiti/app/main.py:121`<br>`services/graphiti/app/main.py:127`<br>`services/graphiti/app/main.py:136` | The Graphiti service requires both inference-proxy credentials and a direct Anthropic API key for LLM extraction. | A deploy can pass proxy configuration but crash on missing Anthropic key, and direct Anthropic calls bypass proxy-level tracing, routing, and rate controls. | Decide whether direct Anthropic is intentional production architecture; if yes, document and monitor that secret separately; if not, fix schema-conforming proxy support and route through the proxy. |
| P3-01 | low | confirmed | `BUILD_STATE.md:126`<br>`Sources/TimedKit/Features/Conversation/ConversationModel.swift:91`<br>`Sources/TimedKit/Core/Services/DeepgramSTTService.swift:58` | `deepgram-transcribe` is local-only while the live conversation path prewarms `deepgram-token` and streams directly to Deepgram WSS. | Operators may deploy or troubleshoot the wrong ASR function because a parked function remains in the tree and docs. | Either delete/park the local-only function outside deployable paths or document that only `deepgram-token` is production speech ingress. |

## Readiness Matrix

| Area | Readiness | Evidence | Production risk | Required before production |
|---|---|---|---|---|
| App | Not ready | Offline replay is not wired (`OfflineSyncQueue.swift:79`, `GracefulDegradation.swift:116`); task sync drops failed remote writes (`DataBridge.swift:58`); onboarding calls authenticated functions with anon headers (`VoiceOnboardingView.swift:242`). | Users can complete flows locally while backend truth and intelligence inputs are missing. | Wire authenticated Edge Function calls, durable retries, visible degraded states, and sync diagnostics. |
| Supabase | Not ready | Generic Anthropic and ElevenLabs proxies are anon-callable; pipeline health has placeholders; cron settings use inconsistent namespaces. | Cost abuse, misleading health, and scheduled job drift. | Lock down proxies, replace placeholder checks, verify cron GUCs and run history, add health probes for deployed functions. |
| Trigger | At risk | Watchdog is unscheduled (`ingestion-health-watchdog.ts:30`); alerting relies on Trigger alert rules watching logs. | Ingestion outages may not page anyone. | Restore schedule or external invocation and test alert delivery end to end. |
| Services | At risk | Graphiti requires direct Anthropic key as well as proxy config (`services/graphiti/app/main.py:118`, `services/graphiti/app/main.py:127`); MCP services have `/readyz` for DB/Neo4j but not all upstream providers. | Service deploys can fail on undocumented secrets or appear ready while provider dependencies are down. | Document required secrets, add provider readiness checks where useful, verify Fly secrets and startup behavior. |
| Packaging | Not ready | Package and notarization scripts disagree on paths and app casing; package script ad-hoc signs. | Release candidate is not a notarized distributable. | Align artifact paths, Developer ID sign, notarize, staple, and verify with Gatekeeper commands. |
| CI | Not ready | CI uploads wrong app path; security scans are advisory; generated Xcode project is not built. | CI can miss app-target, signing, secret, and artifact regressions. | Make gitleaks blocking; add XcodeGen/Xcode build; fix artifact path; add package verification. |
| Docs/tests | At risk | BUILD_STATE conflicts with disabled embedding function; EmailSyncService, memory store, and reflection tests are marked not started. | Operators trust stale readiness claims and high-risk production paths lack regression coverage. | Reconcile docs with deployed state and add targeted tests around cursor, retry, offline, and extraction failure modes. |

## Verification Commands After Fixes

Do not treat this list as commands already run in this review. I did not run builds, tests, deploys, package installs, formatters, or destructive commands.

```bash
git branch --show-current
git status --short --branch

swift build
swift test
swift test --filter EmailSyncServiceTests
swift test --filter GmailSyncServiceTests
swift test --filter OfflineSyncQueueTests

xcodegen generate
xcodebuild -skipMacroValidation -skipPackagePluginValidation ARCHS=arm64 ONLY_ACTIVE_ARCH=YES -scheme TimedMac -configuration Release build

for fn in supabase/functions/*/index.ts; do
  deno check --no-lock "$fn"
done

supabase migration list --linked
supabase functions list --project-ref fpmjuufefhtlwbfinxlx
supabase functions deploy anthropic-proxy --project-ref fpmjuufefhtlwbfinxlx
supabase functions deploy elevenlabs-tts-proxy --project-ref fpmjuufefhtlwbfinxlx
supabase functions deploy extract-onboarding-profile --project-ref fpmjuufefhtlwbfinxlx
supabase functions deploy extract-voice-learnings --project-ref fpmjuufefhtlwbfinxlx
supabase functions deploy pipeline-health-check --project-ref fpmjuufefhtlwbfinxlx

cd trigger
pnpm run typecheck
pnpm run deploy

cd ../services/graphiti-mcp
npm run typecheck
npm run build

cd ../skill-library-mcp
npm run typecheck
npm run build

cd ../..
bash scripts/package_app.sh
bash scripts/create_dmg.sh
codesign --verify --deep --strict dist.noindex/Timed.app
spctl --assess --type execute --verbose dist.noindex/Timed.app
TIMED_NOTARY_PROFILE=timed-notary bash scripts/notarize_app.sh
```

## Review Notes

- Branch verification was read-only: current branch `unified`; status `## unified...origin/unified [ahead 4]` with modified `BUILD_STATE.md` and `SESSION_LOG.md`.
- I did not modify repository files, commit, run builds/tests, install packages, format, deploy, checkout, pull, or run destructive commands.
