import SwiftUI

@main
struct TimeManagerDesktopApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.automatic)
        .defaultSize(width: 1480, height: 940)
    }
}
