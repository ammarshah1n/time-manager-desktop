# Timed Loop-Closure — Definition of Done FAIL

> Superseded on 2026-05-03 by `.timed-loop-closure-SHIPPED.md` after commits `4a118e7`, `56b2b12`, and `e42f07a`. This file is retained as the original failed-gate report.

**Date:** 2026-05-03 09:39 UTC
**Branch:** `unified`

## Wave commits

- Wave 1: `c79559f` — briefing calibration + estimator deepening
- Wave 1.5: `a074135` — calibration helper executive-scoped single-arg fix
- Wave 2: `7641df6` — local estimator activation + `estimate_uncertainty` migration
- Wave 3: `9306234` — task provenance fields + TaskRow badge
- Wave 4: `82c3b79` — one-tap reason chips + event metadata reason patch

## DoD results

1. **FAIL / BLOCKED** — New tasks get `estimated_minutes_ai` populated within 5s.
   - Blocker: remote migration `20260503190000_add_task_estimate_uncertainty.sql` is not applied.
   - `supabase db push --include-all` and `supabase db push --yes` both fail before this migration on pre-existing remote migration drift: `20260501211002_normalize_task_bucket_values.sql` references missing relation `public.bucket_completion_stats`.
   - Current `supabase migration list` shows `20260503190000` local-only.

2. **FAIL / BLOCKED** — Yesterday's overrides surface in the next morning's briefing.
   - Static code is present: `loadCalibrationContext(client, executiveId)` and calibration text injection are wired.
   - Dynamic smoke requires invoking `generate-morning-briefing` with service-role auth and querying `briefings`; current CLI lacks `functions invoke`, and no service-role/psql path is available in this session.

3. **PASS (source-level)** — Estimator pulls signature + recent overrides + time-of-day + calendar density.
   - Verified by grep in `supabase/functions/estimate-time/index.ts`:
     - `Behavioural signature (Tier 2)`
     - `Recent overrides on same bucket (last 30d)`
     - `Time-of-day`
     - `Calendar density`
   - Log-tail verification was not available through the installed Supabase CLI.

4. **PASS** — Provenance pill renders in `TaskRow`.
   - `swift build` passes.
   - Grep verified `provenanceLabel` and `Text(provenanceLabel)` in `TasksPane.swift`.

5. **FAIL / BLOCKED** — Reason chips fire and write `event_metadata.reason` to `behaviour_events`.
   - Static code is present: four `reasonOptions`, `DataBridge.attachReasonToLastOverride`, and Supabase metadata merge `metadata["reason"] = reason`.
   - Dynamic DB verification requires inserting/selecting `behaviour_events`; no direct psql/service-role path is available, and the remote migration drift blocks the planned DB smoke path.

6. **PASS** — `swift build` clean + `swift test` pass.
   - `swift build` completed successfully.
   - `swift test` completed successfully with **95 tests passed** (repo now has more than the plan's older 71-test expectation).

## Exact remote blocker

```text
ERROR: relation "public.bucket_completion_stats" does not exist (SQLSTATE 42P01)
At statement: 5
alter table public.bucket_completion_stats
  drop constraint if exists bucket_completion_stats_bucket_type_check
```

`supabase migration list` tail:

```text
20260501211002 |                | 2026-05-01 21:10:02
20260501211829 |                | 2026-05-01 21:18:29
20260503190000 |                | 2026-05-03 19:00:00
```

## Human ask

Repair/resolve the remote migration drift for `20260501211002_normalize_task_bucket_values.sql` and `20260501211829_task_hierarchy_schema.sql`, then apply `20260503190000_add_task_estimate_uncertainty.sql`. After that, rerun the dynamic DoD items 1, 2, and 5.
