# Timed

[![macOS CI](https://github.com/ammarshah1n/time-manager-desktop/actions/workflows/macos-ci.yml/badge.svg)](https://github.com/ammarshah1n/time-manager-desktop/actions/workflows/macos-ci.yml)
[![Latest Release](https://img.shields.io/github/v/release/ammarshah1n/time-manager-desktop)](https://github.com/ammarshah1n/time-manager-desktop/releases/latest)

Timed is an **executive operating system**, built by and for its co-founders (Yasser + Ammar Shahin) as the prototype's primary users. It is not a productivity app. It builds a compounding cognitive model of how its executive users think and operate, and returns cognitive bandwidth through a nightly reflection engine and a morning intelligence briefing.

> **Mission realignment 2026-04-02:** the original Year-11 study planner framing has been retired. Timed is now the native client for the intelligence engine described in `MASTER-PLAN.md`.

> **Current status 2026-04-30:** `HANDOFF.md` and `BUILD_STATE.md` are the live status files. `MASTER-PLAN.md` remains the architecture plan; older module docs are historical unless their header says otherwise.

## What it is

- macOS 15+ SwiftUI desktop app with shared `TimedKit` and an iOS 18 scaffold on `unified`.
- Ingests Outlook email/calendar via Microsoft Graph (MSAL OAuth) and Gmail/Google Calendar additively via Google Sign-In.
- Nightly Opus-class reflection engine: raw observations → first-order patterns → second-order synthesis → semantic model → procedural rules (Stanford Generative Agents architecture, Park et al. 2023).
- Morning briefing: 7-section CIA PDB format, generated by Opus at max effort.
- 5-tier memory: raw observations / daily summaries / behavioural signatures / personality traits + ACB-FULL and ACB-LIGHT active-context buffers.
- Cognitive layer only — observes, reflects, recommends. Never acts on the world unilaterally.

## Design principles

1. **No cost cap on intelligence.** Use the strongest Opus-class model available for durable-memory generation calls; Trigger.dev Wave 2 aliases route Opus work to 4.7 while legacy Supabase Edge Functions still include direct 4.6 IDs.
2. **Intelligence compounds over time.** Month 6 >> Month 1. This is the moat.
3. **Nightly engine is the heart**, not a nice-to-have feature.
4. **Morning session delivers intelligence**, not a task list.
5. **Users are C-suite executives.** Impressed only by a system that understands them.

## Architecture (current state)

- **UI:** SwiftUI + TCA (`@Dependency` throughout; `@Reducer` in `IntroFeature`)
- **Auth:** `AuthService.swift` — Supabase Auth + Microsoft OAuth + additive Google OAuth
- **Email:** `EmailSyncService` + `GraphClient` for Microsoft; `GmailSyncService` + `GmailClient` for Gmail
- **Backend:** Supabase project `fpmjuufefhtlwbfinxlx`; CLI verified 39 active remote Edge Functions on 2026-04-30; 64 remote migrations applied including `20260430120000_gmail_provider.sql`
- **Embeddings:** Graphiti/skill-library use Voyage-backed embeddings; app-facing `generate-embedding` is currently disabled and returns dimension `0`
- **Orchestration:** pgflow over pgmq, 4-cron schedule (full / refresh / briefing / weekly pruning)

## Canonical documentation

- `CLAUDE.md` — session entry point and rules
- `MASTER-PLAN.md` — full architecture + 13 phases
- `BUILD_STATE.md` — what's built, what's next, verified against CLI
- `PLAN.md` — active sprint
- `AGENTS.md` — repo rules for Claude/Codex
- `docs/01-architecture.md` – `docs/10-phases.md` — module-level docs
- `specs/IMPLEMENTATION_PLAN.md` — 82-task plan, 6 phases
- Research: `research/perplexity-outputs/v2/` (14 intelligence-core reports)
- Obsidian vault: `~/Timed-Brain/` — see `VAULT-INDEX.md`
- AIF canon: `~/Timed-Brain/AIF/` — decision log, A2 chains, stack decisions, session evidence

Historical README fragments (school/study planner, Seqta import, Codex CLI bridge, quiz mode) are archived under `docs-archive/docs-old-20260402/`.

## Local development

```bash
swift build -c release
swift test                      # 49 tests via swift-testing
```

Use XcodeBuildMCP (`build_sim`, `test_sim`) for the canonical build loop — structured JSON errors.

## Distribution

Direct DMG + Sparkle (not App Store). See `scripts/package_app.sh` and `scripts/notarize_app.sh`.

## Trust boundary

- AI routed only via Supabase Edge Functions — no direct client-to-model calls.
- Secrets (Anthropic, Voyage, OpenAI, Graph) live in Supabase secrets + 1Password.
- Timed observes and recommends; the executive always approves world-changing actions.

## Status

Docs refreshed: **2026-05-01**. Gmail backend ops and Graphiti backfill are complete; pending ops are in-app Gmail sign-in, Apple Developer enrollment, and wiring iOS/share/widget/Live Activity/background paths before production iOS claims. See `BUILD_STATE.md` for detail.
